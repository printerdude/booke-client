import React from 'react';

function BookmarkDelete(props) {
    return (
        <div>
            This BookmarkDelete will be a button to delete a specific bookmark
        </div>
    );
}

export default BookmarkDelete;