import React from 'react';

function BookmarkDelete(props) {
// const handleSubmit = (event) => {
//         event.preventDefault();
//         // Behold the power of axios
//         axios.post('http://localhost:3111/icecreams', flavor).then(() => {
//             navigate('/');
//         });

    return (
        <div>
            {/* <form onSubmit={handleSubmit} className='create-form'>
            <label htmlFor='bookmark'>Flavor: </label>
            <input
                onChange={handleChange}
                id='bookmark'
                value={flavor.flavor}
                placeholder='Bookmark'
            />
            <button type='submit'>DELETE</button>
            </form> */}
            this is bookmark delete
        </div>
    );
}

export default BookmarkDelete;